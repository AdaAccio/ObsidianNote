
## 1 总览

 读取 chunks
  → 创建检索器
  → 读取 gold evidence
  → 对每个问题执行 Top-K 检索
  → 比较 source_blocks 与 gold_block_ids
  → 计算 Recall / MRR / nDCG

 BM25 负责关键词匹配，Dense FAISS 负责向量相似度匹配，Hybrid 将两者的归一化分数加权融合；项目通过统一评估脚本比较它们在不同 chunk 策略上的 Recall、MRR 和 nDCG。