```plantUML

@startuml
title Campus Second-hand Trading - Make Payment Activity Diagram

start

partition 学生用户 {
    :浏览商品;
    :选择商品;
    :提交订单;
    :发起支付;
}

partition 平台系统 {
    :生成订单;
    :校验订单信息;
    :请求第三方支付;
}

partition 第三方支付机构 {
    :处理支付请求;
    if (支付成功?) then (Yes)
        :返回支付成功结果;
    else (No)
        :返回支付失败结果;
    endif
}

partition 平台系统 {
    if (支付成功?) then (Yes)
        :更新订单状态为已支付;
        :通知卖家发货/交付;
    else (No)
        :提示支付失败;
        :允许重新支付;
    endif
}

partition 学生用户 {
    if (支付成功?) then (Yes)
        :确认收货;
        :提交评价;
    else (No)
        :重新发起支付或取消订单;
    endif
}

stop

@enduml

```

### 0.1 如何将本项目（HSC-RAG）向当下的 Agent (智能体) 方向演进？

当前项目是一个专注于 RAG 底层基建（层级结构化切块）的项目。要让它更贴合当下火热的 **Agent 岗位的流行方向和技术栈**，可以考虑从单纯的“流水线 (Pipeline)”升级为“智能体工作流 (Agentic Workflow)”。

以下是几个极具简历亮点的优化方向：

#### 0.1.1 方向一：引入 Agentic RAG（智能体 RAG）机制

传统的 RAG 是线性流程（检索 -> 生成），而 Agentic RAG 允许模型自主决策。

- **Query Routing (意图路由)**：引入一个 Router Agent，根据用户问题判断是需要查询知识库（使用本项目的 Chunking 数据），还是直接进行 Web 搜索，或是调用代码解释器。
- **Self-Reflection & CRAG (自我纠错与纠正性检索)**：在检索出 Chunk 后，由一个 Evaluator Agent 判断检索到的 Chunk 是否足以回答问题。如果不足以回答，Agent 可以自主重写 Query 并再次检索。
- **技术栈推荐**：引入 **LlamaIndex Workflows** 或 **LangGraph** 来编排这种包含条件分支和循环的图结构状态机。

#### 0.1.2 方向二：将静态的切块升级为 Multi-Agent 协同处理

目前你们可能是用脚本进行批量 Chunking。可以将其重构为多个专家 Agent 协作：

- **Parsing Agent (解析智能体)**：专门负责处理复杂文档（PDF, 图片），如果遇到复杂的表格，自主调用 OCR 或代码工具进行解析。
- **Summarization Agent (摘要智能体)**：利用当前项目的层级结构，对每一个 Chunk 或父级章节生成高度浓缩的 Metadata。
- **技术栈推荐**：使用 **AutoGen**、**CrewAI** 或底层的 **LangChain** 构建 Multi-Agent 协作网络。

#### 0.1.3 方向三：给 Agent 赋能 Tool Use (工具调用 / Function Calling)

Agent 的核心标志是使用工具。可以将 HSC-RAG 的检索能力封装成工具：

- 不要让用户直接面对 RAG 接口，而是构建一个全能型 Assistant。
- 给这个 Assistant 配备多个工具，比如：`search_chunked_knowledge(query)`、`search_web(query)`、`execute_python(code)`。
- 当用户提问时，LLM 自主决定何时调用 `search_chunked_knowledge`。

#### 0.1.4 方向四：加入长短期记忆 (Memory)

让交互从“单次问答”变成“有上下文的会话”。

- **Short-term Memory**：维护当前会话的上下文，以便进行多轮追问。
- **Long-term Memory / User Persona**：将用户的历史偏好、问过的问题总结后存入图数据库或向量数据库，在每次会话前提取（如 Mem0 库）。

#### 0.1.5 架构层面的建议（技术栈升级）

1. **工作流编排**：从传统的 Python 脚本串行，转为 **LangGraph**。它是目前 Agent 编排最主流的框架，支持状态管理和人类循环（Human-in-the-loop）。
2. **底层大模型接入**：支持主流的推理框架（如 vLLM, Ollama）以及更强大的 API（如支持 Function Calling 的 Claude 3.5 Sonnet / GPT-4o）。
3. **数据库拓展**：目前如果只是存 JSON，可以升级为接入 **Milvus**, **Qdrant** 等现代向量数据库，甚至结合 **Neo4j** 构建 GraphRAG（图谱 RAG 也是目前 Agent 领域的一大热点，非常适合你们这种已经有“层级结构”的数据）。

**你想先从哪个方向切入？** 如果你需要，我可以帮你写一个接入 LangGraph 或封装 Tool Use 的 Demo 方案。